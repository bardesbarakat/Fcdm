
@{
    ViewBag.Title = "NoAccess";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container m-5">
   

        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-6">
                    <div class="card shadow text-center">
                        <div class="card-body">
                            <h4 class="card-title text-warning mb-3">
                                <i class="bi bi-exclamation-triangle-fill"></i> ليس لديك صلاحيات للدخول الي هذا القسم
                            </h4>
                            <p class="card-text">
                                @ViewBag.Message
                            </p>
                            <a href="@Url.Action("Index", "Home")" class="btn btn-outline-primary mt-3">
                                <i class="bi bi-house-door-fill"></i> الرجوع إلى الصفحة الرئيسية
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </div>
   